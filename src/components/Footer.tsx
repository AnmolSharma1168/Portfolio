import { motion } from "framer-motion";

const Footer = () => {
  const currentYear = new Date().getFullYear();

  return (
    <footer className="bg-black">
      {/* Already included in ContactSection - no separate footer needed */}
    </footer>
  );
};

export default Footer;
